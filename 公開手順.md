# アプリを他の人に見せる手順（Streamlit Community Cloud）

離れた場所の人に、アプリとして操作してもらうための手順です。**無料**です。

---

## 準備はもう済んでいます

公開に必要な作業のうち、重い部分は完了しています。

- 株価データ 146MB → **18.9MB に圧縮**（`data/packed/*.parquet`）
- 圧縮版だけでアプリが完全に動くことを確認済み（推奨銘柄はCSV版と完全一致）
- 公開時に不要なファイルを除外する `.gitignore` を用意
- `requirements.txt` を用意
- 銘柄リストをフォルダ内に同梱（外部フォルダへの依存を解消）

あなたにやっていただくのは **GitHubアカウントの用意と、3回のクリック** です。

---

## 手順1: GitHubアカウントを作る（持っていれば飛ばす）

https://github.com/signup で無料登録します。

## 手順2: リポジトリを作ってアップロードする

1. https://github.com/new を開く
2. **Repository name** に `kabu-app` など好きな名前を入れる
3. **Private を選ぶ**（下の「公開範囲」を参照）
4. 「Create repository」

次に、このフォルダをアップロードします。コマンドプロンプトで:

```
cd /d "C:\Users\user\OneDrive\Desktop\claude Code\株価予測長期"
git init
git add .
git commit -m "日本株 長期・配当ベース銘柄選択アプリ"
git branch -M main
git remote add origin https://github.com/あなたのユーザー名/kabu-app.git
git push -u origin main
```

`.gitignore` が効いているので、**生データ146MBはアップロードされません**。
上がるのは圧縮済みの18.9MBとコードだけです。

> Gitが入っていない場合は https://git-scm.com/download/win から入れてください。
> 画面上でファイルをドラッグしてアップロードすることもできますが、
> フォルダ構造を保つ必要があるのでコマンドのほうが確実です。

## 手順3: Streamlit Community Cloud に接続する

1. https://share.streamlit.io を開き、**GitHubアカウントでサインイン**
2. 「Create app」→ 作ったリポジトリを選ぶ
3. **Main file path** に `app.py` と入力
4. 「Deploy」

数分でURL（`https://～.streamlit.app`）が発行されます。
そのURLを相手に送れば、相手はブラウザだけで操作できます。

---

## 公開範囲（ここは必ず決めてください）

| | 誰が見られるか |
|---|---|
| **公開リポジトリ**から作ったアプリ | **URLを知れば誰でも**。検索にも出ます |
| **非公開リポジトリ**から作ったアプリ | **招待した人だけ**。メールアドレスで指定します |

投資判断に使う内容なので、**非公開リポジトリを推奨**します。
Streamlit側の設定画面で、閲覧を許可する人のメールアドレスを登録します。

> 注意: 無料プランで作れる**非公開アプリは同時に1つまで**です。

---

## データは自動で毎日更新されます

`.github/workflows/` に自動更新の設定を入れてあります。
**あなたのPCが起動していなくても、GitHubのサーバーが勝手に更新します。**

| 更新内容 | タイミング | 所要 |
|---|---|---|
| 株価・配当 | **平日 18:00（日本時間）** | 約2分 |
| 決算・銘柄情報 | 毎月1日 5:00 | 約1時間 |

仕組みは次の通りです。

1. GitHub Actions が `update_data.py` を実行する
2. 既に10年分を持っているので、**最後の日付以降だけ**を取りに行く（全部取り直すと30分、増分なら2分）
3. 更新結果を自動でリポジトリに書き戻す
4. Streamlit Cloud が変更を検知して自動で再起動する

**設定は不要です。**リポジトリを作って push した時点で有効になります。
GitHub画面の「Actions」タブで実行履歴を確認でき、「Run workflow」から手動実行もできます。

### データを壊さないための安全装置

Yahooのデータ取得は時々失敗します。実際にテストで **1銘柄（9364）の取得が失敗し、
その銘柄の直近データが空で上書きされて消える** バグが見つかったため、対策を入れました。

- 取得できなかった銘柄は、**既存データをそのまま残す**（空で上書きしない）
- 更新後に既存期間の欠損が増えていたら、**保存せず中止**する
- 最新日に値がある銘柄が全体の80%未満なら、**保存せず中止**する

中止した場合は前回のデータがそのまま使われ続けます。
壊れたデータが公開されることはありません。

### 直近データが後から変わることについて

当日の出来高などは場中に確定していくため、**直近10日ぶんは毎回取り直しています**。
テストでも 2026-08-31 の出来高が 209百万株 → 335百万株 に更新されました。
これは異常ではなく、正しい動作です。

---

## 相手ができること・できないこと

**できる**
- 今日の推奨銘柄と、その理由（検証済み／3年検証／効果なしの区別つき）を見る
- 銘柄数・最低配当利回り・業種上限・割安フィルタを自分で動かす
- 保有銘柄を入力して売却シグナルを確認する
- 過去10年の検証結果とグラフを見る
- 推奨リストをCSVでダウンロードする

**できない**
- 検証結果の再計算（`検証実行.bat` はあなたのPC専用。ただし結果CSVは同梱済み）

株価データは自動更新されるので、**相手は常に最新の推奨を見られます。**

---

## GitHub Actions の無料枠について

- **公開リポジトリ**: 無制限
- **非公開リポジトリ**: 月2,000分まで無料

このアプリの使用量は **毎日2分 × 平日20日 ＋ 月1回60分 ＝ 月およそ100分**。
非公開リポジトリでも無料枠に十分収まります。

---

## 相手に必ず伝えてほしいこと

このアプリは**過去データの検証結果を示すもので、利益を保証しません**。
README（アプリ内の「📖 このツールの限界」タブ）に、以下を明記してあります。

- 10年通した平均で年7%以上が目標。**毎年7%は達成できない**
- 9年中7年で+7%以上。2018年は **-12.0%**、2020年は +5.2% で未達
- 検証期間はすべて日本株の大相場。追い風のサンプルである
- PBRの効果は**3年しか検証できていない**（だから既定では無効）

**相手がそのまま投資判断に使う前に、このタブを読んでもらってください。**
